# timelines-spa

## Local Development

**requirements:**

- Docker and Docker Compose (Docker Desktop comes with both binaries)

**running npm commands e.g. build to populate node_modules for your LSP (bind mount)**
```
docker compose run --rm frontend npm ci
```

**running locally**

```bash
docker compose up --watch
```
